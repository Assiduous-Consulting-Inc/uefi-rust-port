name: Sync Mirror
on:
  schedule:
    - cron: '0 * * * *' # Every hour
  workflow_dispatch: # Allows manual trigger

jobs:
  mirror_sync:
    runs-on: ubuntu-latest
    steps:
      - name: Repo Mirror Sync
        uses: repo-sync/github-sync@v2
        with:
          source_repo: "https://github.com/tianocore/edk2.git"
          github_token: ${{ secrets.SYNC_TOKEN }}
